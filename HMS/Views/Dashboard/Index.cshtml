@model HMS.Models.DashboardViewModel

@{
    ViewData["Title"] = "Dashboard";
}

<div class="dashboard-page">
   @*  <div class="mb-4">
        <h2 class="mb-2">Dashboard</h2>
        <p class="text-muted mb-0">Choose a department, then open one of its screens.</p>
    </div> *@

   @*  @if (Model.SelectedDepartmentId.HasValue)
    {
        <div class="alert alert-light border mb-0">
            Department selected: <strong>@Model.SelectedDepartmentName</strong>.
            Use the screen buttons above to open a module.
        </div>
    }
    else
    {
        <div class="alert alert-secondary mb-0">
            Select a department from the left panel to load its screens.
        </div>
    } *@
</div>
